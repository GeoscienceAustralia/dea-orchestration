# Requirements

This tool requires that the aws cli is installed in for the user account with credentials (located at ~/.aws/credentials) that provide read and write access to the corresponding s3 bucket.
